/// <reference types="vite/client" />

// Replaced at build time by Vite `define`: true only in the single-file build
// (vite.config.single.ts), false in the hosted build and in dev.
declare const __SINGLE_FILE__: boolean;
